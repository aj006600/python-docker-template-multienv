# 本機單環境開發：直接開 localhost:8000 + 熱重載。
# 用法：make dev  → http://localhost:8000
services:
  app:
    # 掛載原始碼 + 熱重載，改程式碼不用重建映像
    volumes:
      - ./app:/app/app
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    ports:
      - "8000:8000"
